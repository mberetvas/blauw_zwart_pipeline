---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# blauw_zwart_fan_sim_pipeline Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-05

## Active Technologies
- Python 3.12+ (`pyproject.toml`) + **None** at runtime for generator (stdlib only per spec/constitution); dev: **pytest**, **ruff** (existing groups). (002-match-calendar-events)
- Filesystem — calendar JSON in, single NDJSON out. (002-match-calendar-events)
- Python **3.12+** (`pyproject.toml` `requires-python`) + **stdlib only** for generator/runtime logic (constitution VI); **`tzdata`** may remain as existing timezone support for v2 (unchanged for v3) (003-ndjson-v3-retail-sim)
- NDJSON **file** (atomic write via existing **`write_atomic_text`**) or **stdout** (stream) (003-ndjson-v3-retail-sim)

- Python 3.12+ (matches `pyproject.toml` `requires-python`) + **None** beyond the Python standard library for the generator script (001-synthetic-fan-events)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.12+ (matches `pyproject.toml` `requires-python`): Follow standard conventions

## Recent Changes
- 003-ndjson-v3-retail-sim: Added Python **3.12+** (`pyproject.toml` `requires-python`) + **stdlib only** for generator/runtime logic (constitution VI); **`tzdata`** may remain as existing timezone support for v2 (unchanged for v3)
- 002-match-calendar-events: Added Python 3.12+ (`pyproject.toml`) + **None** at runtime for generator (stdlib only per spec/constitution); dev: **pytest**, **ruff** (existing groups).

- 001-synthetic-fan-events: Added Python 3.12+ (matches `pyproject.toml` `requires-python`) + **None** beyond the Python standard library for the generator script

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
