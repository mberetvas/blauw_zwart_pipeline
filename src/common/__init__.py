"""Shared infrastructure helpers reused across the repository.

The ``common`` package currently exposes logging utilities that give CLI tools,
HTTP services, and worker processes a consistent structured logging setup.
"""
